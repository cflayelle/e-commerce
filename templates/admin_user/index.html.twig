{% extends 'admin.base.html.twig' %}

{% block title %}Bookshop admin{% endblock %}

{% block body %}
  <div class="pt-5 d-flex flex-column">
    <table class="table table-hover table-light table-striped">
      <thead>
        <tr class="table-dark">
          <th scope="col">id</th>
          <th scope="col">Email</th>
          <th scope="col">Nombre de commandes</th>
          <th scope="col">Actions</th>
        </tr>
      </thead>
      <tbody>
        {% for user in users %}
          <tr class="">
            <th scope="row">{{user.id}}</th>
            <th>{{user.email}}</th>
            <td>{{ user.carts | length - 1 }}</td>
            <td><a class="btn btn-outline-primary btn-sm">Détails</a></td>
          </tr>
        {% endfor %}
      </tbody>
    </table>

  </div>
{% endblock %}
