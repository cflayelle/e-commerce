{% extends 'base.html.twig' %}

{% block title %}Products{% endblock %}

{% block body %}

<div class="example-wrapper">
        <article style="height: 100vh;">
            <div>            
                <img style="width: 400px; height: 400px; object-fit: contain; background-color: #cbd6e4;"  
                    src="{{ vich_uploader_asset(product)}}"
                    alt="">
            </div>
            <div class="p-3 d-flex flex-column align-items-start">
                <h1>{{product.name}}</h1>
                <p><b>{{product.price}} € </b>/ En stock : {{product.stock}}</p>
                <p>{{product.description}}</p>
                <a href="{{path('app_cart_add', {id:product.id})}}" class="btn btn-success d-flex align-items-center justify-content-center">
                    <i class="bi bi-cart-plus"></i>
                </a>

            </div>
        </article>
    
</div>
</div>
{% endblock %}
