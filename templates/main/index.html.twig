{% extends 'base.html.twig' %}

{% block title %}BookShop{% endblock %}

{% block body %}

<div class="home pb-5">
    <div class="header bgc-light d-flex flex-column gap-5 flex-center">
        <div class="container text-center ">
            <h1 class="bienvenue text-center">Bienvenue </h1>
            <a href="{{ path( 'product') }}" class="btn btn-primary">Shopping</a>
        </div>
    </div>

    <div class="category position-relative bgc-light mt-10 container flex-center rounded-10">
        <div class="flex-center position-absolute -top-5  ">
            <span class="text-light bgc-primary py-2 px-5 rounded-max text-center w-auto h3 ">Acheter par catégorie</span>
        </div>
        <div class="mtc-6">
            {% for i in 0..5 %}
                <div>Roman</div>
                <div>Science-fiction</div>
            {% endfor %}
        </div>
    </div>
</div>

{% endblock %}
