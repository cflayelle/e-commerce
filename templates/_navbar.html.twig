<nav class="navbar navbar-expand-lg fixed-top navbar-dark bgc-primary" style="z-index: 100">
  <div class="container d-flex flex-row w-100">
    <a class="navbar-brand text-uppercase" href="{{ path( 'home') }}">BOOKSTORE</a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03"
      aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor03">
      <div class="d-lg-flex justify-content-end align-items-center w-100">
        <div class="d-lg-flex justify-content-between align-items-center w-75">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <a class="nav-link active" href="{{ path( 'home') }}">Accueil</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ path( 'product') }}">Livres</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ path( 'app_category') }}">Catégories</a>
            </li>
            </li>
          </ul>
          <div class="flex">
            <ul class="navbar-nav me-auto d-lg-flex justify-content-lg-center align-items-lg-center">
              <form class="form-inline my-2 my-lg-0" id="form-nav" action="{{ path( 'product') }}" method="get">
                <input name="qName" class="form-control mr-sm-2" type="search" placeholder="Rechercher par nom"
                  aria-label="Search">
                <button class="btn-search my-2 my-sm-0" type="submit">
                  <img src="{{ asset('/images/utils/loupe.png') }}" alt="loupe" width="22">
                </button>
              </form>
              <li class="nav-item d-none d-lg-flex">
                <a class="nav-link" href="{{path('app_cart')}}">
                  <img src="{{ asset('/images/utils/caddie.png') }}" alt="panier" width="32">
                </a>
              </li>

              <div class="dropdown">
                <a href="#" role="button" data-toggle="dropdown" aria-haspopup="true"
                  aria-expanded="false">
                  <img src="{{ asset('/images/utils/avatar.png') }}" alt="avatar" width="26">
                </a>
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuLink">
                  <a class="dropdown-item" href="{{ path('app_cart') }}">Panier</a>
                  <a class="dropdown-item" href="{{ path('app_purchase') }}">Commande</a>
                  <a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
                </div>
                {% else %}
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuLink">
                  <a class="dropdown-item" href="{{path('app_login') }} ">Connexion</a>
                  <a class="dropdown-item" href="{{ path('app_register') }}">S'inscrire</a>
                </div>
                {% endif %}
              </div>
            </ul>
          </div>
        </div>
      </div>
    </div>
</nav>